---
// Final Call-to-Action Section
---

<section class="cta-section" id="contact">
  <div class="cta-container">
    <h2>Start Your Property Documentation Plan Today</h2>
    <p>Join property owners who protect their investments with regular property documentation visits</p>
    <div class="cta-buttons">
      <a href="/checkout" class="btn btn-primary" aria-label="Get started with property inspections">Get Started</a>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: 8rem 3rem;
    position: relative;
    scroll-margin-top: 80px;
  }

  .cta-container {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
    padding: 5rem 3rem;
    background: linear-gradient(135deg,
      rgba(255, 255, 255, 0.05) 0%,
      rgba(255, 255, 255, 0.02) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 32px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
    position: relative;
    overflow: hidden;
  }

  .cta-container::before {
    content: '';
    position: absolute;
    inset: -200px;
    background: radial-gradient(circle, rgba(231, 76, 60, 0.08) 0%, transparent 70%);
    animation: ctaGlow 4s ease-in-out infinite;
    pointer-events: none;
  }

  @keyframes ctaGlow {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
  }

  body.light-mode .cta-container {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(248, 248, 248, 0.9));
    border-color: rgba(231, 76, 60, 0.2);
  }

  .cta-container h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    background: linear-gradient(135deg, #ffffff 0%, #94a3b8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.2s forwards;
  }

  @keyframes ctaFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  body.light-mode .cta-container h2 {
    background: linear-gradient(135deg, #1a1a1a 0%, #333333 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .cta-container p {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 3rem;
    line-height: 1.7;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.4s forwards;
  }

  body.light-mode .cta-container p {
    color: #4a4a4a;
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.6s forwards;
  }

  .btn {
    padding: 1.1rem 2.5rem;
    font-size: 1.05rem;
    font-weight: 600;
    border-radius: 16px;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
  }

  .btn:hover::before {
    width: 300px;
    height: 300px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
    color: white;
    box-shadow: 0 6px 25px rgba(231, 76, 60, 0.35);
  }

  .btn-primary:hover {
    transform: translateY(-4px) scale(1.03);
    box-shadow: 0 12px 35px rgba(231, 76, 60, 0.45);
  }

  .btn-primary:active {
    transform: translateY(-2px) scale(0.98);
  }

  .btn-secondary {
    background: rgba(100, 116, 139, 0.15);
    color: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(100, 116, 139, 0.3);
  }

  .btn-secondary:hover {
    background: rgba(100, 116, 139, 0.25);
    transform: translateY(-4px) scale(1.02);
  }

  .btn-secondary:active {
    transform: translateY(-2px) scale(0.98);
  }

  body.light-mode .btn-secondary {
    background: rgba(0, 0, 0, 0.05);
    color: #1a1a1a;
    border-color: rgba(0, 0, 0, 0.15);
  }

  body.light-mode .btn-secondary:hover {
    background: rgba(0, 0, 0, 0.1);
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: 4rem 1rem;
    }

    .cta-container {
      padding: 2.5rem 1.5rem;
      border-radius: 24px;
    }

    .cta-container h2 {
      font-size: clamp(1.5rem, 6vw, 2rem);
      margin-bottom: 1rem;
    }

    .cta-container p {
      font-size: 1rem;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
      gap: 1rem;
    }

    .btn {
      text-align: center;
      padding: 1rem 2rem;
      font-size: 1rem;
    }
  }

  /* Very small screens */
  @media (max-width: 360px) {
    .cta-section {
      padding: 3rem 0.75rem;
    }

    .cta-container {
      padding: 2rem 1.25rem;
      border-radius: 20px;
    }

    .cta-container h2 {
      font-size: 1.4rem;
    }

    .cta-container p {
      font-size: 0.95rem;
    }

    .btn {
      padding: 0.9rem 1.5rem;
      font-size: 0.95rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cta-container::before {
      animation: none;
    }
  }
</style>
