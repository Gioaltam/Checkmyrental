---
// Hero Section with animations and light effects
---

<section class="hero-section vt-hero">
  <!-- Premium light beam effects with parallax -->
  <div class="light-beams scroll-parallax" data-speed="0.3"></div>
  <div class="scan-light scroll-parallax" data-speed="0.2"></div>

  <!-- Animated gradient overlay with parallax -->
  <div class="hero-gradient-overlay scroll-parallax" data-speed="0.15"></div>

  <div class="hero-content">
    <h1 class="hero-title">
      Property Documentation Services<br>
      <strong>for Tampa Bay Landlords & Property Managers</strong>
    </h1>
    <p class="hero-subtitle">
      Quarterly photo documentation of your rental properties. We photograph visible issues and provide a PDF report with AI-assisted analysis.
    </p>

    <!-- Trust signals -->
    <div class="trust-signals">
      <div class="signal-item">
        <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
        </svg>
        <span>Every issue documented</span>
      </div>
      <div class="signal-item">
        <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
        </svg>
        <span>24-hour PDF delivery</span>
      </div>
      <div class="signal-item">
        <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
        </svg>
        <span>Track changes over time</span>
      </div>
      <div class="signal-item">
        <svg class="check-icon" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
        </svg>
        <span>No contracts required</span>
      </div>
    </div>

    <div class="hero-cta">
      <a href="/pricing" class="btn btn-primary">View Pricing</a>
      <a href="mailto:checkmyrentals.fl@gmail.com?subject=Book%20Property%20Documentation" class="btn btn-secondary">Book Documentation</a>
    </div>

    <p class="disclaimer">
      Visual records that protect your investment.
    </p>
  </div>

  <!-- Scroll cue -->
  <div class="scroll-cue">
    <span>Scroll to explore</span>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 8rem 3rem 4rem;
    overflow: hidden;
  }

  .light-beams {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 200%;
    height: 800px;
    background: linear-gradient(
      180deg,
      transparent 0%,
      rgba(100, 116, 139, 0.02) 20%,
      rgba(100, 116, 139, 0.04) 40%,
      rgba(100, 116, 139, 0.03) 60%,
      rgba(100, 116, 139, 0.02) 80%,
      transparent 100%
    );
    pointer-events: none;
    z-index: 0;
    animation: beamSweep 8s ease-in-out infinite;
  }

  @keyframes beamSweep {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; }
  }

  .scan-light {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(100, 116, 139, 0.5) 50%,
      transparent 100%
    );
    animation: scanMove 4s ease-in-out infinite;
    z-index: 1;
  }

  @keyframes scanMove {
    0%, 100% { left: -100%; opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 1; }
    100% { left: 100%; opacity: 0; }
  }

  .hero-gradient-overlay {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse at 50% 30%,
      rgba(100, 116, 139, 0.08) 0%,
      transparent 60%
    );
    pointer-events: none;
    z-index: 0;
    animation: gradientPulse 6s ease-in-out infinite;
  }

  @keyframes gradientPulse {
    0%, 100% { opacity: 0.5; }
    50% { opacity: 0.8; }
  }

  .hero-content {
    max-width: 1100px;
    text-align: center;
    position: relative;
    z-index: 10;
    padding: 2rem 0;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5.5vw, 4.5rem);
    font-weight: 300;
    line-height: 1.15;
    margin-bottom: 2rem;
    background: linear-gradient(135deg, #ffffff 0%, #94a3b8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.03em;
  }

  .hero-title strong {
    font-weight: 800;
    background: linear-gradient(135deg, #e74c3c 0%, #ff6b6b 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    display: block;
    margin-top: 0.5rem;
  }

  body.light-mode .hero-title {
    background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 50%, #404040 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  body.light-mode .hero-title strong {
    background: linear-gradient(135deg, #e74c3c 0%, #c0392b 50%, #a93226 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-subtitle {
    font-size: clamp(1.05rem, 2vw, 1.25rem);
    color: rgba(255, 255, 255, 0.75);
    line-height: 1.8;
    margin-bottom: 3rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  body.light-mode .hero-subtitle {
    color: #4a4a4a;
  }

  .hero-cta {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    padding: 1rem 2.5rem;
    min-height: 52px;
    font-size: 1.05rem;
    font-weight: 600;
    border-radius: 16px;
    text-decoration: none;
    transition:
      transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1),
      box-shadow 0.3s cubic-bezier(0.16, 1, 0.3, 1),
      background 0.2s ease,
      border-color 0.2s ease;
    position: relative;
    overflow: hidden;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  /* Shimmer effect on hover */
  .btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .btn:hover::before {
    left: 100%;
  }

  .btn:focus {
    outline: 2px solid #e74c3c;
    outline-offset: 3px;
  }

  .btn:focus-visible {
    outline: 2px solid #e74c3c;
    outline-offset: 3px;
    box-shadow: 0 0 0 4px rgba(231, 76, 60, 0.2);
  }

  .btn-primary {
    background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
    color: white;
    box-shadow:
      0 6px 25px rgba(231, 76, 60, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.15);
  }

  .btn-primary:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow:
      0 20px 45px rgba(231, 76, 60, 0.45),
      0 0 30px rgba(231, 76, 60, 0.2),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
  }

  .btn-primary:active {
    transform: translateY(-2px) scale(0.98);
    box-shadow: 0 8px 20px rgba(231, 76, 60, 0.4);
    transition-duration: 0.1s;
  }

  .btn-secondary {
    background: rgba(100, 116, 139, 0.12);
    color: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(100, 116, 139, 0.25);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    background: rgba(100, 116, 139, 0.2);
    transform: translateY(-4px);
    border-color: rgba(231, 76, 60, 0.3);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
  }

  .btn-secondary:active {
    transform: translateY(-2px);
    transition-duration: 0.1s;
  }

  body.light-mode .btn-secondary {
    background: rgba(0, 0, 0, 0.05);
    color: #1a1a1a;
    border-color: rgba(0, 0, 0, 0.12);
  }

  body.light-mode .btn-secondary:hover {
    background: rgba(0, 0, 0, 0.08);
    border-color: rgba(231, 76, 60, 0.3);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }

  .trust-signals {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    justify-content: center;
    margin: 3rem 0 3rem;
  }

  .signal-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.85);
    font-size: 0.95rem;
    font-weight: 500;
    padding: 0.5rem 0.75rem;
    border-radius: 8px;
    transition:
      background 0.3s ease,
      transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1),
      color 0.2s ease;
    cursor: default;
  }

  .signal-item:hover {
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-2px);
    color: rgba(255, 255, 255, 1);
  }

  body.light-mode .signal-item {
    color: rgba(0, 0, 0, 0.75);
  }

  body.light-mode .signal-item:hover {
    background: rgba(0, 0, 0, 0.03);
    color: rgba(0, 0, 0, 0.9);
  }

  .check-icon {
    width: 20px;
    height: 20px;
    color: #e74c3c;
    flex-shrink: 0;
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .signal-item:hover .check-icon {
    transform: scale(1.15);
  }

  .disclaimer {
    margin-top: 3rem;
    margin-bottom: 1rem;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    font-style: italic;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  body.light-mode .disclaimer {
    color: rgba(0, 0, 0, 0.6);
  }

  .scroll-cue {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    z-index: 10;
    pointer-events: none;
  }

  .scroll-cue::after {
    content: '';
    display: block;
    width: 2px;
    height: 40px;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.5), transparent);
    margin: 0.75rem auto 0;
    animation: scrollBounce 2s ease-in-out infinite;
  }

  @keyframes scrollBounce {
    0%, 100% { transform: translateY(0); opacity: 0.5; }
    50% { transform: translateY(10px); opacity: 1; }
  }

  body.light-mode .scroll-cue {
    color: #666666;
  }

  body.light-mode .scroll-cue::after {
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.4), transparent);
  }

  @media (max-width: 768px) {
    .hero-section {
      padding: 6rem 1.5rem 3rem;
    }

    .trust-signals {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
      max-width: 280px;
      margin-left: auto;
      margin-right: auto;
    }

    .signal-item {
      font-size: 0.9rem;
    }

    .hero-cta {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      text-align: center;
    }

    .disclaimer {
      font-size: 0.85rem;
    }

    .scroll-cue {
      bottom: 1.5rem;
      font-size: 0.8rem;
    }

    .scroll-cue::after {
      height: 30px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .light-beams,
    .scan-light,
    .hero-gradient-overlay,
    .scroll-cue::after {
      animation: none;
    }
  }
</style>
