---
import LandingLayout from '../layouts/LandingLayout.astro';
import Header from '../components/Header.astro';
import PricingHero from '../components/PricingHero.astro';
import PricingCards from '../components/PricingCards.astro';
import LandingFooter from '../components/LandingFooter.astro';
---

<LandingLayout
  title="Pricing | Quarterly Property Documentation Visits | CheckMyRental"
  description="Transparent pricing for professional property documentation. Quarterly visits at $100 per property, plus specialized add-on services."
>
  <Header />

  <main id="main-content" tabindex="-1">
    <PricingHero />
    <PricingCards />

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="faq-container">
        <h2>Frequently Asked Questions</h2>

        <div class="faq-grid">
          <div class="faq-item">
            <h3>What exactly do you check?</h3>
            <p>We focus on the things that matter: air filters, inside appliances (oven, dishwasher, refrigerator), under sinks and around toilets for leaks, and any visible damage like broken windows or damaged walls.</p>
          </div>

          <div class="faq-item">
            <h3>How long does a visit take?</h3>
            <p>Usually 30-45 minutes for a typical property. We're focused and efficient — we know what to look for and document it with photos.</p>
          </div>

          <div class="faq-item">
            <h3>Do I need to be there?</h3>
            <p>No. We coordinate directly with your tenants for access. You'll get the PDF report within 24 hours with all the photos and findings.</p>
          </div>

          <div class="faq-item">
            <h3>What if you find a problem?</h3>
            <p>Your report includes a To-Do List that separates tenant fixes from owner fixes. We tag each issue so you know who should handle it and how urgent it is.</p>
          </div>

          <div class="faq-item">
            <h3>Are there any contracts?</h3>
            <p>No contracts. $100 per visit. Cancel at least 24 hours before your scheduled visit for a full refund.</p>
          </div>

          <div class="faq-item">
            <h3>What areas do you service?</h3>
            <p>We serve the Tampa Bay area including St. Petersburg, Clearwater, and surrounding areas. Contact us if you're nearby.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Value Proposition -->
    <section class="value-section">
      <div class="value-container">
        <h2>Why Choose Quarterly Documentation Visits?</h2>
        <div class="value-grid">
          <div class="value-card">
            <div class="value-icon">🛡️</div>
            <h3>Catch Issues Early</h3>
            <p>Regular documentation visits help identify small problems before they become expensive repairs. Save thousands in potential damage costs.</p>
          </div>

          <div class="value-card">
            <div class="value-icon">📸</div>
            <h3>Photo Documentation</h3>
            <p>Comprehensive photo evidence protects you in disputes and helps track property condition over time.</p>
          </div>

          <div class="value-card">
            <div class="value-icon">⚡</div>
            <h3>24-Hour Turnaround</h3>
            <p>Get your detailed report the next day, so you can act quickly on any issues discovered during the documentation visit.</p>
          </div>

          <div class="value-card">
            <div class="value-icon">💰</div>
            <h3>Protect Your Investment</h3>
            <p>At less than $100 per visit, quarterly maintenance is a small price to pay for protecting a major investment.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="cta-container">
        <h2>Ready to Protect Your Property?</h2>
        <p>Start with a quarterly maintenance plan and add any specialized documentation visits you need</p>
        <div class="cta-buttons">
          <button
            class="btn btn-primary"
            onclick="document.querySelector('.pricing-card.featured .add-to-cart-btn').click(); window.scrollTo({top: 0, behavior: 'smooth'});"
            aria-label="Add quarterly maintenance plan to cart"
          >
            Add Quarterly Plan
          </button>
        </div>

        <div class="cta-contact">
          <p class="cta-contact-heading">Questions? Get in touch</p>
          <div class="cta-contact-links">
            <a href="mailto:info@checkmyrental.io" class="cta-contact-link">
              <svg class="contact-icon" viewBox="0 0 20 20" fill="currentColor">
                <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
              </svg>
              info@checkmyrental.io
            </a>
            <a href="tel:7276424457" class="cta-contact-link">
              <svg class="contact-icon" viewBox="0 0 20 20" fill="currentColor">
                <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"/>
              </svg>
              (727) 642-4457
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <LandingFooter />
</LandingLayout>

<style>
  main {
    padding-top: 0;
  }

  /* FAQ Section */
  .faq-section {
    padding: 6rem 3rem;
    background: rgba(255, 255, 255, 0.02);
  }

  body.light-mode .faq-section {
    background: rgba(0, 0, 0, 0.02);
  }

  .faq-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .faq-container h2 {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: rgba(255, 255, 255, 0.95);
  }

  body.light-mode .faq-container h2 {
    color: #1a1a1a;
  }

  .faq-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .faq-item {
    background: linear-gradient(135deg,
      rgba(255, 255, 255, 0.05) 0%,
      rgba(255, 255, 255, 0.02) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    animation: faqFadeIn 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .faq-item:nth-child(1) { animation-delay: 0.1s; }
  .faq-item:nth-child(2) { animation-delay: 0.2s; }
  .faq-item:nth-child(3) { animation-delay: 0.3s; }
  .faq-item:nth-child(4) { animation-delay: 0.4s; }
  .faq-item:nth-child(5) { animation-delay: 0.5s; }
  .faq-item:nth-child(6) { animation-delay: 0.6s; }

  @keyframes faqFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .faq-item:hover {
    transform: translateY(-6px) scale(1.02);
    border-color: rgba(231, 76, 60, 0.3);
    box-shadow: 0 12px 32px rgba(231, 76, 60, 0.15);
  }

  body.light-mode .faq-item {
    background: white;
    border-color: rgba(0, 0, 0, 0.1);
  }

  body.light-mode .faq-item:hover {
    border-color: rgba(231, 76, 60, 0.3);
    box-shadow: 0 8px 24px rgba(231, 76, 60, 0.08);
  }

  .faq-item h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: rgba(255, 255, 255, 0.95);
  }

  body.light-mode .faq-item h3 {
    color: #1a1a1a;
  }

  .faq-item p {
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
    font-size: 0.95rem;
  }

  body.light-mode .faq-item p {
    color: #4a4a4a;
  }

  /* Value Section */
  .value-section {
    padding: 6rem 3rem;
  }

  .value-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .value-container h2 {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: rgba(255, 255, 255, 0.95);
  }

  body.light-mode .value-container h2 {
    color: #1a1a1a;
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .value-card {
    text-align: center;
    padding: 2rem;
    background: linear-gradient(135deg,
      rgba(255, 255, 255, 0.05) 0%,
      rgba(255, 255, 255, 0.02) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    animation: valueFadeIn 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .value-card:nth-child(1) { animation-delay: 0.1s; }
  .value-card:nth-child(2) { animation-delay: 0.2s; }
  .value-card:nth-child(3) { animation-delay: 0.3s; }
  .value-card:nth-child(4) { animation-delay: 0.4s; }

  @keyframes valueFadeIn {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .value-card:hover {
    transform: translateY(-8px) scale(1.03);
    border-color: rgba(231, 76, 60, 0.3);
    box-shadow: 0 12px 32px rgba(231, 76, 60, 0.15);
  }

  body.light-mode .value-card {
    background: white;
    border-color: rgba(0, 0, 0, 0.1);
  }

  .value-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .value-card:hover .value-icon {
    transform: scale(1.15) translateY(-4px);
    animation: iconBounce 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  @keyframes iconBounce {
    0%, 100% { transform: scale(1.15) translateY(-4px); }
    50% { transform: scale(1.2) translateY(-8px); }
  }

  .value-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: rgba(255, 255, 255, 0.95);
  }

  body.light-mode .value-card h3 {
    color: #1a1a1a;
  }

  .value-card p {
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
    font-size: 0.95rem;
  }

  body.light-mode .value-card p {
    color: #4a4a4a;
  }

  /* CTA Section */
  .cta-section {
    padding: 6rem 3rem;
  }

  .cta-container {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    padding: 4rem 3rem;
    background: linear-gradient(135deg,
      rgba(255, 255, 255, 0.05) 0%,
      rgba(255, 255, 255, 0.02) 100%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
    position: relative;
    overflow: hidden;
  }

  .cta-container::before {
    content: '';
    position: absolute;
    inset: -200px;
    background: radial-gradient(circle, rgba(231, 76, 60, 0.08) 0%, transparent 70%);
    animation: ctaGlow 4s ease-in-out infinite;
    pointer-events: none;
  }

  @keyframes ctaGlow {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
  }

  body.light-mode .cta-container {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(248, 248, 248, 0.9));
    border-color: rgba(231, 76, 60, 0.2);
  }

  .cta-container h2 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #ffffff 0%, #94a3b8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.2s forwards;
  }

  @keyframes ctaFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  body.light-mode .cta-container h2 {
    background: linear-gradient(135deg, #1a1a1a 0%, #333333 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .cta-container p {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 2rem;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.4s forwards;
  }

  body.light-mode .cta-container p {
    color: #4a4a4a;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.6s forwards;
  }

  .btn {
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 12px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
  }

  .btn:hover::before {
    width: 300px;
    height: 300px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
    color: white;
    box-shadow: 0 6px 20px rgba(231, 76, 60, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 32px rgba(231, 76, 60, 0.5);
  }

  .btn-primary:active {
    transform: translateY(-1px) scale(0.98);
  }

  .btn-secondary {
    background: rgba(100, 116, 139, 0.15);
    color: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(100, 116, 139, 0.3);
  }

  .btn-secondary:hover {
    background: rgba(100, 116, 139, 0.25);
    transform: translateY(-3px) scale(1.02);
  }

  .btn-secondary:active {
    transform: translateY(-1px) scale(0.98);
  }

  body.light-mode .btn-secondary {
    background: rgba(0, 0, 0, 0.05);
    color: #1a1a1a;
    border-color: rgba(0, 0, 0, 0.15);
  }

  body.light-mode .btn-secondary:hover {
    background: rgba(0, 0, 0, 0.1);
  }

  .cta-contact {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: ctaFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.8s forwards;
  }

  body.light-mode .cta-contact {
    border-top-color: rgba(0, 0, 0, 0.1);
  }

  .cta-contact-heading {
    font-size: 1rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 1rem;
  }

  body.light-mode .cta-contact-heading {
    color: #333333;
  }

  .cta-contact-links {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    align-items: center;
  }

  .cta-contact-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    font-size: 0.95rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    padding: 0.5rem 1rem;
    border-radius: 8px;
  }

  .cta-contact-link:hover {
    color: #e74c3c;
    background: rgba(231, 76, 60, 0.1);
    transform: translateX(4px);
  }

  body.light-mode .cta-contact-link {
    color: rgba(0, 0, 0, 0.7);
  }

  body.light-mode .cta-contact-link:hover {
    color: #e74c3c;
    background: rgba(231, 76, 60, 0.08);
  }

  .contact-icon {
    width: 18px;
    height: 18px;
    flex-shrink: 0;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .cta-contact-link:hover .contact-icon {
    transform: scale(1.15) rotate(5deg);
  }

  @media (max-width: 768px) {
    .faq-grid {
      grid-template-columns: 1fr;
    }

    .value-grid {
      grid-template-columns: 1fr;
    }

    .faq-section,
    .value-section,
    .cta-section {
      padding: 3rem 1.5rem;
    }

    .faq-container h2,
    .value-container h2,
    .cta-container h2 {
      font-size: 2rem;
    }

    .cta-container {
      padding: 2.5rem 1.5rem;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cta-container::before {
      animation: none;
    }
  }
</style>
